{
  "compilerOptions": {
    "jsx": "react",
    "baseUrl": "./src/typey-boi/",
    "paths": {
      "actions": ["actions"],
      "components": ["components"],
      "constants": ["constants"],
      "containers": ["containers"],
      "contexts": ["contexts"],
      "hooks": ["hooks"],
      "models": ["models"],
      "reducers": ["reducers"],
      "store": ["store"]
    }
  },
  "exclude": ["node_modules", ".cache", "build", "dist"]
}